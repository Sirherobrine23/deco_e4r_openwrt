#!/bin/sh

if [ "$POINT" == "END" -a "$DSTMODE" == "FAP" ]; then
	/etc/init.d/qos stop &
	sleep 5
	/etc/init.d/qos start &
elif [ "$POINT" = "END" -a "$DSTMODE" = "RE" ]; then
	/etc/init.d/qos stop &
fi